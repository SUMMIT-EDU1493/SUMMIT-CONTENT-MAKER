import OpenAI from "openai";

export async function POST(request: Request) {
  try {
    const apiKey = process.env.OPENAI_API_KEY;

    if (!apiKey) {
      return Response.json(
        { error: "OPENAI_API_KEY가 설정되지 않았습니다." },
        { status: 500 }
      );
    }

    const openai = new OpenAI({ apiKey });

    const body = await request.json();
    const { title, content } = body;

    if (!content || typeof content !== "string") {
      return Response.json(
        { error: "대화문 내용이 없습니다." },
        { status: 400 }
      );
    }

    const response = await openai.responses.create({
      model: "gpt-5-mini",
      input: `
아래 영어 대화문을 바탕으로 한국 중학생용 "써밋네컷" 4컷 만화 설계안을 만들어라.

핵심 목표:
- 원문의 핵심 의미는 유지한다.
- 직역하거나 해석본처럼 만들지 않는다.
- 실제 한국 중학생 친구들이 말할 법한 자연스러운 대사로 재구성한다.
- 짧고 생생한 만화 대사를 만든다.

[말투 규칙]
1. 등장인물은 기본적으로 친구, 같은 반 학생, 또래 청소년이다.
2. 반드시 자연스러운 반말을 사용한다.
3. "~요", "~습니다", "~네요", "~해요" 같은 존댓말은 금지한다.
4. 번역투를 금지한다.
5. 설명문이나 보고서 같은 문장은 금지한다.
6. 실제 말로 했을 때 어색한 문장은 만들지 않는다.
7. 문장은 짧고 자연스럽고 리듬감 있게 만든다.

[절대 금지하는 어색한 표현]
- "너면 꼭 될 거야."
- "너라면 진짜 될 거야."
- "추천직업이 실용적인 편이야."
- "나는 네가 성공할 수 있다고 확신해."
- "정말 좋네요."
- "그것은 좋은 선택인 것 같아."
- "나는 그것이 적절하다고 생각해."

[좋은 표현 예시]
- "대박이다!"
- "와, 진짜?"
- "너한테 딱인데?"
- "넌 진짜 잘할 것 같아!"
- "완전 잘 어울린다!"
- "이 직업(job), 너한테 잘 맞는 것 같아."
- "네 성격(personality)이랑 딱 맞네."
- "넌 꼭 멋진 선수가 될 거야!"
- "같이 해보자!"

[자연스러움 최종 점검]
대사를 만든 뒤 반드시 스스로 확인한다.
- 실제 친구가 입으로 말해도 자연스러운가?
- 번역투가 남아 있지 않은가?
- 너무 딱딱하거나 설명조이지 않은가?
- 의미는 분명하면서 짧은가?
어색하면 출력 전에 자연스럽게 다시 고친다.

[영어 학습 요소]
1. 원문에서 중요한 단어나 표현 4~6개 정도를 고른다.
2. 영어는 반드시 해당 한국어 뜻 바로 뒤에 괄호로 붙인다.
3. 형식은 반드시 "한글(English)"이다.
4. 예:
   - 성격(personality)
   - 직업(job)
   - 계획(plan)
   - 자신감(confidence)
5. 영어만 문장 끝에 따로 붙이지 않는다.
6. 한국어와 영어를 다른 줄에 분리하지 않는다.
7. 긴 영어 문장을 그대로 넣지 않는다.
8. 핵심 학습어만 자연스럽게 대사 안에 넣는다.

[4컷 구성]
1. 반드시 4컷으로 구성한다.
2. 각 컷은 앞뒤 흐름이 자연스럽게 이어져야 한다.
3. 한 컷의 대사는 1~2개 정도만 사용한다.
4. 대사는 실제 말풍선에 바로 넣을 수 있을 정도로 짧게 만든다.
5. 장면 설명은 이미지 생성 AI가 이해할 수 있도록 구체적으로 작성한다.
6. 등장인물의 외모와 복장은 4컷 동안 일관되게 유지할 수 있도록 작성한다.

[표정과 행동]
- 질문하는 화자는 궁금해하거나 묻는 표정을 짓는다.
- 대답하는 사람은 물음표 표정을 짓지 않는다.
- 질문이 있는 컷에서는 누가 질문하는지 scene에도 명확히 표시한다.
- 답변하는 사람에게 질문 표시가 붙지 않도록 장면을 명확하게 설명한다.
- 감정과 몸짓은 실제 대사의 의미와 일치해야 한다.

[화자]
1. 각 대사에 speaker를 반드시 지정한다.
2. 한 컷에서 두 명이 말하면 각각 별도의 dialogue 항목으로 만든다.
3. speaker는 이름 또는 명확한 인물 구분을 사용한다.
4. 이미지 AI가 헷갈리지 않도록 scene에 누가 질문하고 누가 대답하는지도 명확히 적는다.

[한줄요약]
- summary는 완성 이미지 상단에 들어갈 한글 제목이다.
- 8~18자 정도.
- 짧고 깔끔한 교재 주제처럼 만든다.
- 예:
  "성격 유형과 직업"
  "장래희망과 재능"
  "꿈과 적성 찾기"

[출력]
반드시 JSON만 출력한다.
마크다운, 설명문, 코드블록은 금지한다.

{
  "title": "${title || "써밋네컷"}",
  "summary": "짧은 한글 한줄요약",
  "panels": [
    {
      "cut": "1컷",
      "scene": "장면, 표정, 행동, 누가 질문/대답하는지까지 구체적으로 설명",
      "characters": "등장인물의 외모와 복장",
      "dialogue": [
        {
          "speaker": "화자",
          "text": "자연스러운 대사"
        }
      ]
    },
    {
      "cut": "2컷",
      "scene": "장면, 표정, 행동, 누가 질문/대답하는지까지 구체적으로 설명",
      "characters": "등장인물의 외모와 복장",
      "dialogue": [
        {
          "speaker": "화자",
          "text": "자연스러운 대사"
        }
      ]
    },
    {
      "cut": "3컷",
      "scene": "장면, 표정, 행동, 누가 질문/대답하는지까지 구체적으로 설명",
      "characters": "등장인물의 외모와 복장",
      "dialogue": [
        {
          "speaker": "화자",
          "text": "자연스러운 대사"
        }
      ]
    },
    {
      "cut": "4컷",
      "scene": "장면, 표정, 행동, 누가 질문/대답하는지까지 구체적으로 설명",
      "characters": "등장인물의 외모와 복장",
      "dialogue": [
        {
          "speaker": "화자",
          "text": "자연스러운 대사"
        }
      ]
    }
  ]
}

원문 대화문:
${content}
`,
    });

    const raw = response.output_text
      .replace(/```json/gi, "")
      .replace(/```/g, "")
      .trim();

    const parsed = JSON.parse(raw);

    return Response.json(parsed);
  } catch (error: any) {
    console.error("COMIC PLAN ERROR:", error);

    return Response.json(
      {
        error: "써밋네컷 설계안 생성 중 오류가 발생했습니다.",
        detail:
          error?.message ||
          error?.error?.message ||
          "알 수 없는 오류",
      },
      { status: 500 }
    );
  }
}